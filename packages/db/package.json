{
  "name": "@siders/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/client.ts",
  "types": "./src/client.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "drizzle-orm": "^0.32.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@siders/config": "workspace:*",
    "@types/pg": "^8.11.6",
    "drizzle-kit": "^0.23.0",
    "typescript": "^5.5.3",
    "vitest": "^2.0.2"
  }
}
